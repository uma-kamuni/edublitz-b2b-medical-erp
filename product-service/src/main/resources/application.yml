server:
  port: 8082
  servlet:
    context-path: /api/v1

spring:
  application:
    name: product-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://cbz:redhat@edublitz.hbvwsdw.mongodb.net/products_db?appName=edublitz}
      database: products_db
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false

jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.edublitz: INFO

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:*}

stock:
  low-threshold: ${LOW_STOCK_THRESHOLD:10}
